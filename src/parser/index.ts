// Code to ingest the events based on definitions, and analyse the entities and the relationships between them
import * as yaml from 'js-yaml';

const definitions_location = "./events.yaml";

const read_events = (loc: string): any => {
  return '';
};

export default function() {
  // Read the events location from somewhere
  // Find all the entity's attributes
  // Check the logic of the events
  // Create a dependency tree for all the attributes
  // Something
}
